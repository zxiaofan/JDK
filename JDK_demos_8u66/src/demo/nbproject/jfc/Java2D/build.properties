main.dir=${basedir}/../../../jfc/Java2D

src.dir=${main.dir}/src

build.dir=build
classes.dir=${build.dir}/classes
jar=${main.dir}/Java2D.jar
javadoc.dir=${build.dir}/javadoc

build.sysclasspath=ignore
# E.g.: cp=lib/x.jar:lib/y.jar
cp=
extra.run.cp=

main.class=java2d.Java2Demo

run.cp=${cp}:${classes.dir}:${extra.run.cp}

debug=true
deprecation=false

nbjdk.home=${basedir}/../../../..

applet.html=${main.dir}/Java2Demo.html
